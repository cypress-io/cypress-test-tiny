{
  "name": "cypress-test-tiny",
  "version": "1.0.0",
  "description": "Repro for cypress-io/cypress#34807 — clientCertificates ignored on the browser network path",
  "private": true,
  "scripts": {
    "certs": "bash ./make-certs.sh",
    "server": "node server.js",
    "cypress:run": "cypress run --browser chrome",
    "cypress:open": "cypress open"
  },
  "license": "ISC",
  "devDependencies": {
    "cypress": "16.0.0"
  }
}
