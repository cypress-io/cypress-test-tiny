<form action="/" method="POST">
  <fieldset>
    <legend>{{i.signUp.legend}}</legend>
    <label>
      {{i.signUp.email}}: <input type="email" data-cy="email-input" name="email" />
    </label>
    <button data-cy="signup-btn">Sign Up</button>
  </fieldset>
</form>